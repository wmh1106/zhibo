<view class='writeInfo'>
    <textarea bindinput="changeText" auto-focus="{{true}}" maxlength="140" placeholder="请填写直播介绍" value="{{val}}" />
    <view bind:tap="createContent" class="endButton">完成</view>
</view>